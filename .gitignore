# ── 构建产物 ──
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/
/.task/
/build/bin/
/build/*.deb
/build/*.rpm
/build/*.sha256
/build/deb/usr/bin/
/build/deb/usr/share/doc/lumin/*.gz
/build/x86_64/
/build/windows/installer/Output/
/build/windows/installer/wails_tools.nsh
*.syso

# ── Wails 自动生成 ──
# v3 迁移：wailsjs/ 现在是手写的 v3 兼容 shim（指向 frontend/bindings 生成代码），
# 必须入库；v3 生成绑定在 frontend/bindings/，随仓库提交以便 CI 直接构建。
/frontend/package.json.md5

# ── 前端 ──
/frontend/node_modules/
/frontend/dist/
/frontend/dist_old_*/

# ── WorkBuddy 本地数据 ──
.workbuddy/

# ── 编辑器 / IDE ──
.trae/
.idea/
.vscode/
.zcode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── 日志、临时文件 ──
*.log
*.tmp
.DS_Store

# ── 本地配置（不上传） ──
.env*
*.bat
配置目录/

# ── Superpowers 临时会话 ──
.superpowers/
